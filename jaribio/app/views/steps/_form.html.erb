<%= form_for([@test_case, @step], :remote => true) do |f| %>
  <% if @step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% @step.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :action %><br />
    <%= f.text_area :action, :rows => 3, :columns => 50 %>
  </div>
  <div class="field">
    <%= f.label 'Expected results' %><br />
    <%= f.text_area :results, :rows => 3, :columns => 50 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
